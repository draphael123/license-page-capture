import Image from "next/image";
const shots=[
 {src:"/screenshots/extension-setup.png",title:"Set up a test",text:"Name the run, confirm where files will be saved, review preflight, and open Settings and tools only when needed.",points:["Use a non-sensitive test name","Confirm Ready to test","Select Start test"]},
 {src:"/screenshots/extension-capturing.png",title:"Monitor the capture",text:"The active view shows the number saved, recent activity, skipped sensitive pages, notes, and the manual capture fallback.",points:["Wait for transition confirmed","Add operational notes","Finish when the workflow is complete"]},
 {src:"/screenshots/extension-review.png",title:"Review the record",text:"The completed view summarizes saved, skipped, and failed events and provides a readiness score before reports are exported.",points:["Inspect every exception","Open the screenshots","Create the session summary"]},
];
export default function ExtensionScreenshots(){return <section className="real-screens" aria-labelledby="actual-interface"><header><p>Actual extension interface · v1.1.0</p><h2 id="actual-interface">What you will see in the browser.</h2><span>These screenshots are rendered from the current extension files using fictional data. They are not redesigned marketing mockups.</span></header><div>{shots.map((shot,index)=><figure key={shot.title}><div className="shot-frame"><Image src={shot.src} width={760} height={1520} unoptimized alt={`Page Capture extension ${shot.title.toLowerCase()} screen`}/></div><figcaption><b>{index+1}. {shot.title}</b><p>{shot.text}</p><ul>{shot.points.map(point=><li key={point}>{point}</li>)}</ul></figcaption></figure>)}</div></section>}
