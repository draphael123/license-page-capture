"use client";
import {useMemo,useState} from "react";
const rows=[["Normal HTML forms","Verified","Automatic"],["Single-page applications","Supported","Transition confirmation"],["Open Shadow DOM","Supported","Deep control detection"],["Long scrolling forms","Supported","Full-page with fallback"],["Custom text buttons","Configurable","Portal profile"],["Programmatic form submits","Supported","Capture interception"],["Cross-origin iframes","Limited","Manual review required"],["Browser settings and PDF viewers","Blocked by browser","Not capturable"]];
export default function Directory(){const[q,setQ]=useState("");const filtered=useMemo(()=>rows.filter(r=>r.join(" ").toLowerCase().includes(q.toLowerCase())),[q]);return <section className="compat-search" aria-labelledby="directory-title"><div className="directory-tools"><div><p className="overline">Compatibility directory</p><h2 id="directory-title">Search by portal behavior.</h2></div><label>Filter patterns<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Try iframe, long page, custom button…"/></label></div><div className="compat-directory"><div className="compat-head"><span>Portal pattern</span><span>Status</span><span>Behavior</span></div>{filtered.map(r=><div key={r[0]}><strong>{r[0]}</strong><b>{r[1]}</b><span>{r[2]}</span></div>)}</div><p className="directory-count">Showing {filtered.length} of {rows.length} tested patterns.</p></section>}
