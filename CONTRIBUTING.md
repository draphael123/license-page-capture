# Contributing

Use synthetic application data only. Never commit provider information, screenshots from real applications, credentials, portal exports, or identity documents.

Before proposing a change, run:

```bash
npm test
```

Portal-specific rules should include the board domain, synthetic fixtures, expected navigation labels, sensitive-screen behavior, and a regression test.
